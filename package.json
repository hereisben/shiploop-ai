{
  "name": "shiploop-ai",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev -w @shiploop/api",
    "build:api": "npm run build -w @shiploop/api",
    "typecheck:api": "npm run typecheck -w @shiploop/api"
  },
  "devDependencies": {
    "prisma": "^7.8.0"
  },
  "dependencies": {
    "@prisma/client": "^7.8.0"
  }
}
